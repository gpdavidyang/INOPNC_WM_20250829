# Vercel Build Environment Variables
# This file ensures correct settings during Vercel build process

# CRITICAL: Disable auth bypass in ALL deployments
NEXT_PUBLIC_DEV_AUTH_BYPASS=false

# Force production mode
NODE_ENV=production

# Supabase Configuration (these should be set in Vercel dashboard)
# NEXT_PUBLIC_SUPABASE_URL=<set-in-vercel-dashboard>
# NEXT_PUBLIC_SUPABASE_ANON_KEY=<set-in-vercel-dashboard>