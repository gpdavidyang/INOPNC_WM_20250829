# Production Environment Override
# This file ensures critical security settings in production

# CRITICAL: Authentication bypass must NEVER be enabled in production
NEXT_PUBLIC_DEV_AUTH_BYPASS=false

# Ensure production mode
NODE_ENV=production